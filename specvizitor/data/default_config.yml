catalogue:
  translate:
    id: [Id, ID, NUMBER]
    ra: [Ra, RA]
    dec: [Dec, DEC]
    z: [Z, redshift, Redshift, REDSHIFT, z_map]

data:
  translate:
    wavelength:
      - wave
      - lambda
    flux_error:
      - std
      - err
      - fluxerr
      - flux_err
      - fluxerror

review_form:
  default_checkboxes:
    contamination: "Contamination"
    extended: "Extended emission"

plugins:
  - grizli