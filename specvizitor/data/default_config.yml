cat:
  translate:
    id: [Id, ID, NUMBER]
    ra: [Ra, RA]
    dec: [Dec, DEC]
    z: [Z, redshift, Redshift, z_map]

data:
  dir: '.'
  id_pattern: '\d+'
  translate:
    wavelength:
      - wave
      - lambda
    flux_error:
      - std
      - err
      - fluxerr
      - flux_err
      - fluxerror

appearance:
  antialiasing: False

control_panel:
  button_width: 130

object_info:
  items:
    ra: "RA: {}"
    dec: "Dec: {}"

review_form:
  checkboxes:
    contamination: "Contamination"
    extended: "Extended emission"

viewer_geometry:
  spacing: 5
  margins: 5

plugins:
  - grizli