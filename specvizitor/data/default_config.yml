catalogue:
  translate:
    id: [Id, ID, NUMBER]
    redshift: [z, Z, Redshift, REDSHIFT, z_vac, z_map]

review_form:
  default_checkboxes:
    contamination: "Contamination"
    extended: "Extended emission"

plugins:
  - grizli