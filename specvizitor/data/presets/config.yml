catalogue:
  translate:
    id: [Id, ID, NUMBER]
    redshift: [z, Z, Redshift, REDSHIFT, z_vac, z_map]

data:
  enabled_unit_aliases:
    ANGSTROMS: AA

review_form:
  default_flags:
    - contamination
    - extended

plugins:
  - grizli